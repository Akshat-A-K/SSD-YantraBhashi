export class SubmissionService {

    validateInputCode(input) {
        const isValid = false;
        const errors = [];
        // write the logic for validating a string of code
        return { isValid, errors };
    }
}